const valores = [10, 7  ];

for (let contador = 0; contador < valores.length; contador++) {
    console.log('Mostrando todos os elementos ', valores[contador]);
    if(contador == 3) {
        console.log('Mostrando o 4º termo: ', valores[contador]);
    }
}
